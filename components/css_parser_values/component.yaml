name: css_parser_values
version: 0.1.0
type: core
language: rust
estimated_lines: 8000
description: CSS value and property parsing - units, colors, media queries

dependencies:
  imports: [{"name": "css_types"}]

public_api:
  - src/lib.rs

private_implementation:
  - src/_internal/

test_directories:
  - tests/unit
  - tests/integration
  - benches

quality_standards:
  min_test_coverage: 80
  enforce_tdd: true
