[package]
name = "css-matcher-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
# No external dependencies needed

[dev-dependencies]
# Test dependencies for comprehensive testing
