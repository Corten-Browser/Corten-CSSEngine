[package]
name = "css-parser-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
# Component dependencies
css-types = { path = "../css_types" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"

[dev-dependencies]
# For testing
criterion = "0.5"
